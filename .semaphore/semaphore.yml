version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      secrets:
        - name: google-maps-api-key
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
            - echo "My Google Maps API key is $GOOGLE_MAPS_API_KEY"
